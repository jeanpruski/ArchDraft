services:
  db:
    image: postgres:16-alpine
    container_name: archdraft-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: archdraft
    ports:
      - "5432:5432"
